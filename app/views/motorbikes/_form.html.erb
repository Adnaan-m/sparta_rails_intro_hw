<%= form_with(model: motorbike, local: true) do |form| %>
  <% if motorbike.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(motorbike.errors.count, "error") %> prohibited this motorbike from being saved:</h2>

      <ul>
      <% motorbike.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
